import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  buttonsContainer: {
    position: "absolute",
    bottom: "0"
  },
  floorButtons: {
    display: "block",
    cursor: "pointer",
    height: "25px",
    width: "25px",
    textAlign: "center"
  }
});

export default useStyles;
